---
import "../styles/global.css";

const cryptoCoins = [
  "BTC",
  "ETH",
  "USDT",
  "USDC",
  "DAI",
  "UXD",
  "USDP",
  "WLD",
  "BNB",
  "SOL",
  "XRP",
  "ADA",
  "AVAX",
  "DOGE",
  "TRX",
  "LINK",
  "DOT",
  "MATIC",
  "SHIB",
  "LTC",
  "BCH",
  "EOS",
  "XLM",
  "FTM",
  "AAVE",
  "UNI",
  "ALGO",
  "BAT",
  "PAXG",
  "CAKE",
  "AXS",
  "SLP",
  "MANA",
  "SAND",
  "CHZ",
].sort();
const fiatCurrencies = [
  "ARS",
  "BRL",
  "CLP",
  "COP",
  "MXN",
  "PEN",
  "VES",
  "BOB",
  "UYU",
  "DOP",
  "PYG",
  "USD",
  "EUR",
].sort();
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Crypto Dash Pro 🚀</title>
  </head>
  <body
    class="bg-slate-900 min-h-screen flex items-center justify-center p-4 text-slate-200 font-sans"
  >
    <div
      class="w-full max-w-lg bg-slate-800/40 backdrop-blur-xl border border-slate-700/50 p-6 md:p-10 rounded-[2.5rem] shadow-2xl"
    >
      <header class="text-center mb-8">
        <h1
          class="text-3xl md:text-4xl font-black text-white tracking-tighter mb-2"
        >
          Crypto <span class="text-orange-500">Dash</span> 🚀
        </h1>
        <p class="text-slate-400 text-sm">Consulta de Precios Real-Time</p>
      </header>

      <form id="cryptoForm" class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="flex flex-col">
          <label
            class="text-[10px] font-black text-orange-500 uppercase tracking-[0.2em] mb-2 ml-1"
            >Cripto</label
          >
          <select
            name="coin"
            class="bg-slate-900/80 border-2 border-slate-700 rounded-2xl p-4 text-white outline-none focus:border-orange-500 appearance-none"
          >
            {cryptoCoins.map((coin) => <option value={coin}>{coin}</option>)}
          </select>
        </div>

        <div class="flex flex-col">
          <label
            class="text-[10px] font-black text-orange-500 uppercase tracking-[0.2em] mb-2 ml-1"
            >Fiat</label
          >
          <select
            name="fiat"
            class="bg-slate-900/80 border-2 border-slate-700 rounded-2xl p-4 text-white outline-none focus:border-orange-500 appearance-none"
          >
            {
              fiatCurrencies.map((fiat) => (
                <option value={fiat} selected={fiat === "PEN"}>
                  {fiat}
                </option>
              ))
            }
          </select>
        </div>

        <div class="flex flex-col md:col-span-2">
          <label
            class="text-[10px] font-black text-orange-500 uppercase tracking-[0.2em] mb-2 ml-1"
            >Volumen</label
          >
          <input
            type="number"
            name="volumen"
            step="0.000001"
            value="0.1"
            class="bg-slate-900/80 border-2 border-slate-700 rounded-2xl p-4 text-white outline-none focus:border-orange-500"
          />
        </div>

        <button
          type="submit"
          class="md:col-span-2 w-full bg-orange-600 hover:bg-orange-500 text-white font-black py-5 rounded-2xl shadow-xl transition-all uppercase tracking-widest text-sm"
        >
          Consultar Ahora
        </button>
      </form>

      <div
        id="status"
        class="mt-8 p-4 rounded-2xl text-center text-xs font-bold hidden border"
      >
      </div>
    </div>

    <script>
      const form = document.getElementById("cryptoForm") as HTMLFormElement;
      const status = document.getElementById("status");
      // USA ESTA URL DE NGROK (ASEGÚRATE QUE N8N ESTÉ ACTIVO)
      const WEBHOOK_URL =
        "https://epimeric-cleansable-miah.ngrok-free.dev/webhook/cotizacion-crypto";

      form.addEventListener("submit", async (e) => {
        e.preventDefault();
        const formData = new FormData(form);
        const data = Object.fromEntries(formData);

        if (status) {
          status.className =
            "mt-8 p-4 rounded-2xl text-center text-xs font-bold block border bg-orange-500/10 text-orange-400 border-orange-500/30";
          status.innerText = "⏳ ENVIANDO...";
        }

        try {
          // EL TRUCO ESTÁ AQUÍ: mode 'no-cors' y el header de ngrok
          await fetch(WEBHOOK_URL, {
            method: "POST",
            mode: "no-cors",
            headers: {
              "Content-Type": "application/json",
              "ngrok-skip-browser-warning": "true",
            },
            body: JSON.stringify(data),
          });

          // Con no-cors no podemos leer la respuesta, así que mostramos éxito directamente
          if (status) {
            status.className =
              "mt-8 p-4 rounded-2xl text-center text-xs font-bold block border bg-green-500/10 text-green-400 border-green-500/30";
            status.innerText = "✅ ENVIADO CORRECTAMENTE A TELEGRAM";
          }
        } catch (err) {
          if (status) {
            status.className =
              "mt-8 p-4 rounded-2xl text-center text-xs font-bold block border bg-red-500/10 text-red-400 border-red-500/30";
            status.innerText = "❌ ERROR AL ENVIAR";
          }
        }
      });
    </script>
  </body>
</html>
